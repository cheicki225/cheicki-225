# --- WebSocket & réseau ---
websockets==12.0
aiohttp==3.9.5
aiodns==3.2.0

# --- Env / config ---
python-dotenv==1.0.1

# --- ML (pour train_arbitrage_model.py, lancé manuellement, pas requis
# pour le fonctionnement continu du bot tant que le filtre n'est pas branché) ---
xgboost==2.1.0
scikit-learn==1.5.1
pandas==2.2.2
numpy==1.26.4
