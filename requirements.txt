# --- WebSocket & réseau ---
websockets==12.0
aiohttp==3.9.5
aiodns==3.2.0

# --- Métriques système (pour l'app mobile) ---
psutil==6.0.0

# --- Env / config ---
python-dotenv==1.0.1

# --- ML : entraînement manuel (train_arbitrage_model.py) ET scoring en
# continu par le bot (filtre_ml.py, si modele_arbitrage.json est présent) ---
xgboost==2.1.0
scikit-learn==1.5.1
pandas==2.2.2
numpy==1.26.4
